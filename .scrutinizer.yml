filter:
  excluded_paths: [doc/*, log/*, res/*, vendor/*]

tools:
  external_code_coverage:
    enabled: true
    timeout: 600
  php_code_sniffer:
    enabled: true
    filter:
      excluded_paths: [doc/*, log/*, res/*, vendor/*]
  php_mess_detector:
    enabled: true
    filter:
      excluded_paths: [doc/*, log/*, res/*, vendor/*]
  php_cpd:
    enabled: true
    excluded_dirs: [doc/*, log/*, res/*, vendor/*]
    filter:
      excluded_paths: [doc/*, log/*, res/*, vendor/*]
  php_hhvm:
    enabled: true
    filter:
      excluded_paths: [doc/*, log/*, res/*, vendor/*]
  php_loc:
    enabled: true
    excluded_dirs: [doc/*, log/*, res/*, vendor/*]
  php_pdepend:
    enabled: true
    excluded_dirs: [doc/*, log/*, res/*, vendor/*]
  php_analyzer:
    enabled: true
    filter:
      excluded_paths: [doc/*, log/*, res/*, vendor/*]
